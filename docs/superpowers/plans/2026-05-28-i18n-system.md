# i18n system implementation plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task.

**Goal:** Ship a centralized i18n system for `@eocrm/design-system` (React Context + hook + en/ru defaults + deep-merge override). Migrate every component with hard-coded UI strings or an existing `labels?` prop. Per the spec at `docs/superpowers/specs/2026-05-28-i18n-system-design.md`.

**Branch:** `feat/i18n-system` (already checked out off main).

**Architecture (locked in via brainstorm):**
- Nested message objects with TS-typed keys
- Simple template literals + per-locale plural branches (no ICU)
- Manual sweep + typed registry (no extraction tool for v1)
- Per-component `labels?` props are DELETED — i18n provider is the new override surface
- Aria-labels routed through the same system

---

## Task 1: i18n infrastructure

**Files:**

- Create: `packages/design-system/src/i18n/messages.ts`
- Create: `packages/design-system/src/i18n/format.ts`
- Create: `packages/design-system/src/i18n/en.ts` (skeleton — populated as components migrate)
- Create: `packages/design-system/src/i18n/ru.ts` (skeleton)
- Create: `packages/design-system/src/i18n/I18nProvider.tsx`
- Create: `packages/design-system/src/i18n/useTranslation.ts`
- Create: `packages/design-system/src/i18n/index.ts`
- Create: `packages/design-system/src/i18n/format.test.ts`
- Create: `packages/design-system/src/i18n/useTranslation.test.tsx`
- Modify: `packages/design-system/src/index.ts` — re-export `I18nProvider`, `useTranslation`, `Locale`, `Messages`, `MessageKey`, `DeepPartial`.

**Implementation:**

### `messages.ts`

```ts
// Empty interface to start — components add their keys as they migrate.
// Each leaf is either a string (static) or a function (parameterized).
export interface Messages {
  // Will be populated cluster by cluster. See spec §"Messages shape".
}

export type Locale = 'en' | 'ru';

export type DeepPartial<T> = {
  [K in keyof T]?: T[K] extends (...args: never[]) => unknown
    ? T[K]
    : T[K] extends object
      ? DeepPartial<T[K]>
      : T[K];
};

type Leaf = string | ((...args: never[]) => string) | readonly string[];

export type MessageKey<T = Messages> = T extends Leaf
  ? ''
  : { [K in keyof T & string]: T[K] extends Leaf ? K : `${K}.${MessageKey<T[K]>}` }[keyof T & string];
```

### `format.ts`

```ts
import type { DeepPartial } from './messages';

export function deepMerge<T extends Record<string, unknown>>(base: T, override: DeepPartial<T>): T {
  const result: Record<string, unknown> = { ...base };
  for (const key in override) {
    const baseV = result[key];
    const overV = override[key];
    if (
      baseV != null &&
      overV != null &&
      typeof baseV === 'object' &&
      typeof overV === 'object' &&
      !Array.isArray(baseV) &&
      !Array.isArray(overV) &&
      typeof baseV !== 'function' &&
      typeof overV !== 'function'
    ) {
      result[key] = deepMerge(baseV as Record<string, unknown>, overV as DeepPartial<Record<string, unknown>>);
    } else if (overV !== undefined) {
      result[key] = overV;
    }
  }
  return result as T;
}

export function lookupKey(messages: Record<string, unknown>, key: string): unknown {
  return key.split('.').reduce<unknown>((acc, part) => {
    if (acc != null && typeof acc === 'object') return (acc as Record<string, unknown>)[part];
    return undefined;
  }, messages);
}

/**
 * Russian plural rule. CLDR categories: one / few / many. Used inside ru.ts
 * for any message that varies by count.
 *
 * Examples:
 *   ruPlural(1,  ['файл', 'файла', 'файлов']) → 'файл'
 *   ruPlural(3,  ['файл', 'файла', 'файлов']) → 'файла'
 *   ruPlural(11, ['файл', 'файла', 'файлов']) → 'файлов'
 *   ruPlural(22, ['файл', 'файла', 'файлов']) → 'файла'
 */
export function ruPlural(n: number, forms: readonly [string, string, string]): string {
  const m10 = n % 10;
  const m100 = n % 100;
  if (m10 === 1 && m100 !== 11) return forms[0];
  if (m10 >= 2 && m10 <= 4 && (m100 < 10 || m100 >= 20)) return forms[1];
  return forms[2];
}
```

### `I18nProvider.tsx`

```tsx
import { createContext, useMemo, type ReactNode } from 'react';
import type { DeepPartial, Locale, Messages } from './messages';
import { en } from './en';
import { ru } from './ru';
import { deepMerge } from './format';

interface I18nContextValue {
  locale: Locale;
  messages: Messages;
}

const LOCALE_MAP: Record<Locale, Messages> = { en, ru };

// Default context = English with no overrides. Components used WITHOUT a
// provider still work and render English copy.
export const I18nContext = createContext<I18nContextValue>({ locale: 'en', messages: en });

export interface I18nProviderProps {
  /** Locale code. v1 ships 'en' and 'ru'. */
  locale: Locale;
  /** Optional deep-partial overrides applied over the locale defaults. */
  overrides?: DeepPartial<Messages>;
  children: ReactNode;
}

export function I18nProvider({ locale, overrides, children }: I18nProviderProps) {
  const value = useMemo<I18nContextValue>(() => {
    const base = LOCALE_MAP[locale];
    const messages = overrides ? deepMerge(base, overrides) : base;
    return { locale, messages };
  }, [locale, overrides]);
  return <I18nContext.Provider value={value}>{children}</I18nContext.Provider>;
}
```

### `useTranslation.ts`

```ts
import { useContext, useMemo } from 'react';
import { I18nContext } from './I18nProvider';
import type { MessageKey } from './messages';
import { lookupKey } from './format';

export function useTranslation() {
  const { messages } = useContext(I18nContext);
  return useMemo(() => {
    return function t<K extends MessageKey>(key: K, params?: Record<string, unknown>): string {
      const leaf = lookupKey(messages, key as string);
      if (leaf == null) {
        if (typeof console !== 'undefined') console.warn('[i18n] missing key:', key);
        return key as string;
      }
      if (typeof leaf === 'function') return (leaf as (p: Record<string, unknown>) => string)(params ?? {});
      if (typeof leaf === 'string') return leaf;
      // Array leaves (months, weekdays) — caller indexes; we return the raw.
      // TS-side, the caller should know which keys are arrays. At runtime we
      // coerce to a JSON string so a misuse is visible.
      return JSON.stringify(leaf);
    };
  }, [messages]);
}

/** Convenience for components that need raw array messages (months / weekdays). */
export function useTranslationArray() {
  const { messages } = useContext(I18nContext);
  return useMemo(() => {
    return function ta(key: string): readonly string[] {
      const leaf = lookupKey(messages, key);
      return Array.isArray(leaf) ? (leaf as readonly string[]) : [];
    };
  }, [messages]);
}
```

### `en.ts` and `ru.ts` (skeleton)

Empty objects to start. Both files exist so they can be imported by `I18nProvider`. Populated section-by-section as each component migrates in subsequent tasks.

```ts
// en.ts
import type { Messages } from './messages';
export const en: Messages = {} as Messages;

// ru.ts
import type { Messages } from './messages';
export const ru: Messages = {} as Messages;
```

(`as Messages` is a deliberate cheat for the skeleton — the interface starts empty so the cast is sound. As components migrate and add to `Messages`, the en/ru files become genuinely required to populate.)

### Tests

`format.test.ts` covers:
- `deepMerge` with strings, objects, arrays, functions
- `lookupKey` with valid path, invalid path, deeply-nested path
- `ruPlural` for 1, 2, 5, 11, 21, 22, 0 → correct form

`useTranslation.test.tsx` covers:
- Returns en defaults when no provider
- Returns ru defaults inside `<I18nProvider locale="ru">`
- Applies overrides over the locale
- Returns the key (and console.warns) for a missing key
- Calls function leaves with params

- [ ] Step 1: write all files above
- [ ] Step 2: `npm test -w @eocrm/design-system -- --run i18n` — must pass
- [ ] Step 3: `make build`, `make lint` — clean
- [ ] Step 4: commit
  ```
  git commit -m "i18n: provider, hook, format helpers (en/ru skeleton)"
  ```

---

## Task 2: Inline aria-label cluster

Migrate the components with simple inline `aria-label` strings. Each gets:
- A new section added to the `Messages` interface
- The string added to `en.ts` (and `ru.ts` — both populated together)
- Component replaces `aria-label="X"` with `aria-label={t('comp.key')}`

**Components:** Alert, Toast, ToastViewport, Modal/Header, Drawer/Header, Kanban, ColorPicker (SVSquare + ColorPickerPanel), ImageCrop, FileUpload (Check icon's "Done").

For each component, the migration is mechanical:
1. Read its source to find the hard-coded string
2. Add the key to `Messages` interface
3. Add value to `en.ts` and `ru.ts`
4. Replace inline string with `t('...')` (and add `useTranslation` import)
5. Update or add a test asserting the translated string renders (using the provider with an override)

**Reference messages added in this task:**

```ts
// messages.ts add:
alert: { dismiss: string };
colorPicker: {
  saturationBrightness: string;
  hue: string;
  hexValue: string;
  presetColors: string;
};
drawer: { close: string };
fileUpload: { done: string };
imageCrop: { zoom: string };
kanban: { board: string };
modal: { close: string };
toast: { dismiss: string; notifications: string };
```

Mapping table to fill `en.ts` / `ru.ts`:

| Key | en | ru |
| --- | --- | --- |
| `alert.dismiss` | `Dismiss` | `Закрыть` |
| `colorPicker.saturationBrightness` | `Saturation and brightness` | `Насыщенность и яркость` |
| `colorPicker.hue` | `Hue` | `Оттенок` |
| `colorPicker.hexValue` | `Hex color value` | `Hex-значение цвета` |
| `colorPicker.presetColors` | `Preset colors` | `Предустановленные цвета` |
| `drawer.close` | `Close dialog` | `Закрыть диалог` |
| `fileUpload.done` | `Done` | `Готово` |
| `imageCrop.zoom` | `Zoom` | `Масштаб` |
| `kanban.board` | `Kanban board` | `Канбан-доска` |
| `modal.close` | `Close dialog` | `Закрыть диалог` |
| `toast.dismiss` | `Dismiss` | `Закрыть` |
| `toast.notifications` | `Notifications` | `Уведомления` |

- [ ] Step 1: migrate the 9 components
- [ ] Step 2: gates green
- [ ] Step 3: commit
  ```
  git commit -m "i18n: migrate inline aria-labels (9 components)"
  ```

---

## Task 3: Pagination + Select + OptionsPicker + DataTable + small visible-text

These have BOTH inline aria-labels AND visible text. Slightly more complex than Task 2.

**Components:** Pagination, Select (Trigger + Listbox), OptionsPicker, DataTable.

**Reference messages added in this task:**

```ts
dataTable: {
  selectAll: string;
  rowExpansion: string;
  pinnedRows: string;
  empty: string;
};
optionsPicker: {
  filter: string;       // also used as aria-label for the search input
  apply: string;
  cancel: string;
  noMatches: string;
};
pagination: {
  previous: string;          // visible button text
  next: string;              // visible button text
  previousAriaLabel: string; // aria-label
  nextAriaLabel: string;     // aria-label
};
select: {
  clear: string;
  search: string;
  noOptions: string;
};
```

Mapping table:

| Key | en | ru |
| --- | --- | --- |
| `dataTable.selectAll` | `Select all rows on page` | `Выбрать все строки на странице` |
| `dataTable.rowExpansion` | `Row expansion` | `Развёртывание строки` |
| `dataTable.pinnedRows` | `Pinned rows` | `Закреплённые строки` |
| `dataTable.empty` | `No data` | `Нет данных` |
| `optionsPicker.filter` | `Filter…` | `Фильтр…` |
| `optionsPicker.apply` | `Apply` | `Применить` |
| `optionsPicker.cancel` | `Cancel` | `Отмена` |
| `optionsPicker.noMatches` | `No matches` | `Совпадений не найдено` |
| `pagination.previous` | `Previous` | `Назад` |
| `pagination.next` | `Next` | `Далее` |
| `pagination.previousAriaLabel` | `Previous page` | `Предыдущая страница` |
| `pagination.nextAriaLabel` | `Next page` | `Следующая страница` |
| `select.clear` | `Clear selection` | `Очистить выбор` |
| `select.search` | `Search…` | `Поиск…` |
| `select.noOptions` | `No options` | `Нет вариантов` |

**For OptionsPicker, ConfirmationPopover (Task 6), and the date-picker family (Task 5)**: the existing `cancelLabel` / `applyLabel` / `searchPlaceholder` props are DELETED. The `labels` prop is removed too. JSDoc and `*Props` interfaces are cleaned up.

- [ ] Step 1: migrate
- [ ] Step 2: gates green
- [ ] Step 3: commit
  ```
  git commit -m "i18n: Pagination + Select + OptionsPicker + DataTable"
  ```

---

## Task 4: Calendar (months + weekdays + UI strings)

The biggest single migration — 12 months + 7 weekdays + several other strings. Uses array messages (the `useTranslationArray` helper or just indexes via `lookupKey` directly).

**Messages:**

```ts
calendar: {
  today: string;
  agendaEmpty: string;
  allDay: string;
  months: readonly [string, string, string, string, string, string, string, string, string, string, string, string];
  weekdaysShort: readonly [string, string, string, string, string, string, string];
};
```

en months: January … December.
ru months: Январь, Февраль, Март, Апрель, Май, Июнь, Июль, Август, Сентябрь, Октябрь, Ноябрь, Декабрь.
en weekdaysShort: Mon, Tue, Wed, Thu, Fri, Sat, Sun.
ru weekdaysShort: Пн, Вт, Ср, Чт, Пт, Сб, Вс.
en today: `Today`, agendaEmpty: `No events`, allDay: `All day`.
ru today: `Сегодня`, agendaEmpty: `Нет событий`, allDay: `Весь день`.

Delete the `labels?: CalendarLabels` prop from `CalendarProps`. Delete `DEFAULT_LABELS` and `CalendarLabels` type. Update Calendar.tsx, MonthView.tsx, AgendaView.tsx to use `useTranslation` + `useTranslationArray` for the array messages.

- [ ] Step 1: migrate
- [ ] Step 2: gates green
- [ ] Step 3: commit
  ```
  git commit -m "i18n: Calendar (months, weekdays, UI strings) — drop labels prop"
  ```

---

## Task 5: DatePicker family (DatePicker, InlineDatePicker, DateRangePicker, InlineDateRangePicker)

All four share a similar `labels` prop pattern. Migrate them together.

**Messages:**

```ts
datePicker: {
  today: string;
  clear: string;
  openCalendar: string;
  previousMonth: string;
  nextMonth: string;
  rangeStart: string;
  rangeEnd: string;
};
```

Mapping:

| Key | en | ru |
| --- | --- | --- |
| `datePicker.today` | `Today` | `Сегодня` |
| `datePicker.clear` | `Clear` | `Очистить` |
| `datePicker.openCalendar` | `Open calendar` | `Открыть календарь` |
| `datePicker.previousMonth` | `Previous month` | `Предыдущий месяц` |
| `datePicker.nextMonth` | `Next month` | `Следующий месяц` |
| `datePicker.rangeStart` | `Start date` | `Дата начала` |
| `datePicker.rangeEnd` | `End date` | `Дата окончания` |

Each component: read its existing `DEFAULT_LABELS` for the actual set of keys; merge into `datePicker.*` namespace if any are missing. Delete `labels` prop, `DEFAULT_LABELS`, `*Labels` types. Replace `resolvedLabels.X` with `t('datePicker.X')`.

- [ ] Step 1: migrate all 4
- [ ] Step 2: gates green
- [ ] Step 3: commit
  ```
  git commit -m "i18n: DatePicker / DateRangePicker family — drop labels prop"
  ```

---

## Task 6: PasswordInput + PasswordStrengthMeter + ConfirmationPopover

Last of the existing `labels`-prop components.

**Messages:**

```ts
confirmationPopover: { cancel: string };
passwordInput: { show: string; hide: string };
passwordStrengthMeter: { weak: string; fair: string; strong: string; veryStrong: string };
```

Mapping:

| Key | en | ru |
| --- | --- | --- |
| `confirmationPopover.cancel` | `Cancel` | `Отмена` |
| `passwordInput.show` | `Show password` | `Показать пароль` |
| `passwordInput.hide` | `Hide password` | `Скрыть пароль` |
| `passwordStrengthMeter.weak` | `Weak` | `Слабый` |
| `passwordStrengthMeter.fair` | `Fair` | `Средний` |
| `passwordStrengthMeter.strong` | `Strong` | `Надёжный` |
| `passwordStrengthMeter.veryStrong` | `Very strong` | `Очень надёжный` |

Delete `labels` props and `DEFAULT_LABELS` consts in each.

- [ ] Step 1: migrate the 3
- [ ] Step 2: gates green
- [ ] Step 3: commit
  ```
  git commit -m "i18n: PasswordInput + PasswordStrengthMeter + ConfirmationPopover — drop labels prop"
  ```

---

## Task 7: Sweep playground for newly-broken usages

Search the playground for any consumer of the deleted `labels`/`cancelLabel`/`applyLabel`/`searchPlaceholder` props. Each becomes either:
- A removal of the prop (uses i18n defaults from now on)
- Or wrapped in `<I18nProvider overrides={...}>` if the consumer's custom labels were doing real work

Likely affected: any mockup or demo using OptionsPicker with `cancelLabel`/`applyLabel`/`searchPlaceholder`; Calendar/DatePicker demos with `labels`.

Also: wrap `packages/playground/src/App.tsx` content in `<I18nProvider locale="en">` to demonstrate the API in the playground.

- [ ] Step 1: grep `labels=` / `cancelLabel=` / `applyLabel=` / `searchPlaceholder=` across playground; update each
- [ ] Step 2: wrap App.tsx in `<I18nProvider>`
- [ ] Step 3: `make build`, `make lint`, `make test` green
- [ ] Step 4: commit
  ```
  git commit -m "playground: adapt to i18n refactor; wrap App in I18nProvider"
  ```

---

## Task 8: Docs

- `packages/design-system/AGENTS.md` — new "Localization" section near the top: explain the provider + hook + override pattern + key shape + how to add a new string.
- `packages/design-system/CLAUDE.md` — new Hard rule (Rule 9): "Every new user-facing string goes through `useTranslation`. No new inline `aria-label="..."` / placeholder / visible text on any component. Add to `i18n/messages.ts` + `en.ts` + `ru.ts`."

- [ ] Step 1: write docs
- [ ] Step 2: commit
  ```
  git commit -m "Docs: i18n system in AGENTS.md + new Hard rule 9 in CLAUDE.md"
  ```

---

## Task 9: Library hr8 review-fix loop

Per `CLAUDE.md` Hard rule 8 — library-scope change requires a fresh-context review cycle before push.

- [ ] Step 1: run all gates (`make test`, `make build`, `make lint`, `npm pack --dry-run -w @eocrm/design-system`)
- [ ] Step 2: spawn fresh-context `general-purpose` reviewer on the full diff. Brief on the 10 categories. Specifically probe: are there ANY hard-coded English strings still in any component? Are all locale fallbacks safe (missing-key returns key + warn)? Does `<I18nProvider>` correctly memoize so children don't re-render on parent re-renders that don't change `locale`/`overrides`?
- [ ] Step 3: fix Critical + Important findings
- [ ] Step 4: re-run gates + re-spawn reviewer until verdict is `clean enough to stop`

---

## Task 10: Push + PR

- [ ] Step 1: final gate sweep
- [ ] Step 2: push (handle prettier pre-push if it flags anything)
- [ ] Step 3: `gh pr create` with title "Centralized i18n: provider + hook + en/ru translations; drop labels props"
- [ ] Step 4: report PR URL
