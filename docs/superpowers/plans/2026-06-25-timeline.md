# `<Timeline>` Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship a `<Timeline>` + `<Timeline.Item node>` activity-feed primitive (vertical connector line through per-item node slots, content to the right, line stops at the last node, compact variant) to `@eocrm/design-system`, resolving issue eocrm/design-system#203.

**Architecture:** A compound `forwardRef` primitive — `<ol>` root + `<li>` items, each a 2-column grid `[gutter | content]`. The gutter holds a fixed-height node box (centers the consumer `node`) and an absolutely-positioned connector line; `:last-child` CSS hides the connector on the last item. `compact` sets `--timeline-*` CSS vars on the root that items read via the cascade. Tokens-only; no JS last-detection; no i18n.

**Tech Stack:** TypeScript, React, SCSS modules, Vitest + Testing Library.

**Spec:** `docs/superpowers/specs/2026-06-25-timeline-design.md`

---

## File map

Library (`packages/design-system/`):

- `src/components/Timeline/Timeline.tsx` (new) — root + Item.
- `src/components/Timeline/Timeline.module.scss` (new).
- `src/components/Timeline/Timeline.tokens.scss` (new).
- `src/components/Timeline/Timeline.test.tsx` (new).
- `src/components/Timeline/index.ts` (new).
- `src/index.ts` (modify) — exports.
- `src/_meta/manifest.ts` + `scripts/generate-manifest.mjs` (modify) — `Timeline: 'Display'`; then `npm run build:manifest`.
- `AGENTS.md` (modify) — TL;DR.

Playground (`packages/playground/`):

- `src/pages/components/TimelineDemo.tsx` (new).
- `src/App.tsx` / `src/layout/AppShell/navItems.ts` / `src/pages/components/ComponentsIndex.tsx` / `src/pages/mockups/registry.ts` (modify) — wiring.

---

## Task 1: `Timeline` component (TDD)

**Files:** Create `Timeline.tokens.scss`, `Timeline.module.scss`, `Timeline.tsx`, `index.ts`, `Timeline.test.tsx`.

- [ ] **Step 1: Write `Timeline.test.tsx`**

Vitest globals on. CSS-module class names keep their source name as a substring.

```tsx
import { createRef } from 'react';
import { render, screen } from '@testing-library/react';
import { Timeline } from './Timeline';

function basic() {
  return render(
    <Timeline>
      <Timeline.Item node={<span data-testid="node-a">A</span>}>
        <span data-testid="content-a">first</span>
      </Timeline.Item>
      <Timeline.Item node={<span>B</span>}>second</Timeline.Item>
    </Timeline>,
  );
}

describe('Timeline', () => {
  it('renders an <ol> with one <li> per item', () => {
    const { container } = basic();
    expect(container.querySelector('ol')).not.toBeNull();
    expect(container.querySelectorAll('li')).toHaveLength(2);
  });

  it('renders each item node and content', () => {
    basic();
    expect(screen.getByTestId('node-a')).toBeInTheDocument();
    expect(screen.getByTestId('content-a')).toBeInTheDocument();
    expect(screen.getByText('second')).toBeInTheDocument();
  });

  it('renders a connector element in every item (the last-stop is CSS :last-child)', () => {
    const { container } = basic();
    // Both items carry a connector node; CSS (.item:last-child .connector{display:none})
    // hides it on the last — the DOM still contains it.
    expect(container.querySelectorAll('[class*="connector"]')).toHaveLength(2);
  });

  it('compact adds the compact class to the <ol>', () => {
    const { container } = render(
      <Timeline compact>
        <Timeline.Item node={<span>A</span>}>x</Timeline.Item>
      </Timeline>,
    );
    expect((container.querySelector('ol') as HTMLElement).className).toMatch(/compact/);
  });

  it('forwards refs to the <ol> and <li>', () => {
    const olRef = createRef<HTMLOListElement>();
    const liRef = createRef<HTMLLIElement>();
    render(
      <Timeline ref={olRef}>
        <Timeline.Item ref={liRef} node={<span>A</span>}>
          x
        </Timeline.Item>
      </Timeline>,
    );
    expect(olRef.current?.tagName).toBe('OL');
    expect(liRef.current?.tagName).toBe('LI');
  });

  it('merges className and spreads attrs on both root and item', () => {
    const { container } = render(
      <Timeline className="root-c" data-testid="tl">
        <Timeline.Item className="item-c" node={<span>A</span>} data-kind="x">
          x
        </Timeline.Item>
      </Timeline>,
    );
    const ol = container.querySelector('ol') as HTMLElement;
    const li = container.querySelector('li') as HTMLElement;
    expect(ol.className).toMatch(/root-c/);
    expect(ol).toHaveAttribute('data-testid', 'tl');
    expect(li.className).toMatch(/item-c/);
    expect(li).toHaveAttribute('data-kind', 'x');
  });

  it('Timeline.Item renders standalone (no provider needed)', () => {
    expect(() => render(<Timeline.Item node={<span>A</span>}>x</Timeline.Item>)).not.toThrow();
  });
});
```

- [ ] **Step 2: Run → FAIL** (`cd packages/design-system && npx vitest run src/components/Timeline/Timeline.test.tsx`).

- [ ] **Step 3: Create `Timeline.tokens.scss`**

```scss
:root {
  --timeline-gutter: var(--size-md); // 32px node column
  --timeline-node-size: var(--size-md); // 32px fixed node box
  --timeline-row-gap: var(--space-4); // 16px between items
  --timeline-content-gap: var(--space-3); // 12px node → content
  --timeline-line-width: var(--border-width); // hairline connector
  --timeline-line-color: var(--color-border);

  --timeline-gutter-compact: var(--size-sm); // 24px
  --timeline-node-size-compact: var(--size-sm); // 24px
  --timeline-row-gap-compact: var(--space-2); // 8px
  --timeline-content-gap-compact: var(--space-2); // 8px
}
```

- [ ] **Step 4: Create `Timeline.module.scss`**

```scss
@use './Timeline.tokens';

// stylelint-disable property-disallowed-list -- internal grid + connector positioning,
// not consumer-facing layout

.root {
  // UA <ol> reset.
  list-style: none;
  margin: 0;
  padding: 0;
  display: block;
}

.item {
  display: grid;
  grid-template-columns: var(--timeline-gutter) 1fr;
  column-gap: var(--timeline-content-gap);
}

.gutter {
  position: relative;
}

.nodeBox {
  height: var(--timeline-node-size);
  display: flex;
  align-items: center;
  justify-content: center;
}

.connector {
  position: absolute;
  left: 50%;
  transform: translateX(-50%);
  top: var(--timeline-node-size);
  bottom: 0;
  width: var(--timeline-line-width);
  background: var(--timeline-line-color);
}

.item:last-child .connector {
  display: none;
}

.content {
  min-width: 0;
  padding-block-end: var(--timeline-row-gap);
}

.item:last-child .content {
  padding-block-end: 0;
}

.compact {
  --timeline-gutter: var(--timeline-gutter-compact);
  --timeline-node-size: var(--timeline-node-size-compact);
  --timeline-row-gap: var(--timeline-row-gap-compact);
  --timeline-content-gap: var(--timeline-content-gap-compact);
}
```

(If stylelint flags the `<ol>` `margin`/`padding` despite the file-level disable, add a scoped `// stylelint-disable-next-line property-disallowed-list` above each, mirroring `SortableGroup.module.scss`. Verify `--size-md`/`--size-sm`/`--border-width`/`--color-border`/`--space-*` exist — they do.)

- [ ] **Step 5: Create `Timeline.tsx`**

```tsx
import { forwardRef, type HTMLAttributes, type ReactNode } from 'react';
import clsx from 'clsx';
import styles from './Timeline.module.scss';

export interface TimelineProps extends HTMLAttributes<HTMLOListElement> {
  /** Tighter gutter, node box, and spacing for dense sidebar widgets. Default `false`. */
  compact?: boolean;
  /** `<Timeline.Item>`s. */
  children: ReactNode;
}

export interface TimelineItemProps extends Omit<HTMLAttributes<HTMLLIElement>, 'children'> {
  /**
   * The gutter node — an `<Avatar>`, `<Dot>`, icon, etc. Centered in a fixed node box so
   * the connector aligns regardless of node content.
   */
  node: ReactNode;
  /** The item content (right of the node) — e.g. name·type·time, body, system text. */
  children: ReactNode;
}

/**
 * Vertical activity-feed primitive — a connector line running between per-item `node`
 * slots (avatar / dot / icon) with content to the right. The line connects consecutive
 * nodes and **stops at the last node**. Use `<Timeline compact>` for a dense sidebar widget.
 *
 * @example
 * <Timeline>
 *   {activities.map((a) => (
 *     <Timeline.Item key={a.id} node={<Avatar name={a.actor} size="sm" src={a.avatarUrl} />}>
 *       <Text size="sm"><strong>{a.actor}</strong> · {a.type} · {a.time}</Text>
 *       <Text size="sm" tone="muted">{a.body}</Text>
 *     </Timeline.Item>
 *   ))}
 * </Timeline>
 *
 * @example
 * // Compact sidebar widget with dot nodes:
 * <Timeline compact>
 *   <Timeline.Item node={<Dot tone="success" />}>
 *     <Text size="sm">Renewal confirmed</Text>
 *   </Timeline.Item>
 * </Timeline>
 *
 * @remarks When NOT to use
 * - A plain vertical list with no connector / nodes → `<Stack>`.
 * - A horizontal step indicator → not this (Timeline is vertical).
 *
 * @remarks Anti-patterns
 * - ❌ Expecting a built-in dot — `node` is a slot; pass `<Dot>` / `<Avatar>` / an icon.
 * - ❌ Hand-rolling the connector line — it's built in and stops at the last item automatically.
 * - ❌ Putting layout margins on items — spacing comes from `compact` / the row gap token.
 */
const TimelineRoot = forwardRef<HTMLOListElement, TimelineProps>(function Timeline(
  { compact = false, className, children, ...rest },
  ref,
) {
  return (
    <ol ref={ref} className={clsx(styles.root, compact && styles.compact, className)} {...rest}>
      {children}
    </ol>
  );
});

const TimelineItem = forwardRef<HTMLLIElement, TimelineItemProps>(function TimelineItem(
  { node, className, children, ...rest },
  ref,
) {
  return (
    <li ref={ref} className={clsx(styles.item, className)} {...rest}>
      <div className={styles.gutter}>
        <div className={styles.nodeBox}>{node}</div>
        <span className={styles.connector} aria-hidden="true" />
      </div>
      <div className={styles.content}>{children}</div>
    </li>
  );
});
TimelineItem.displayName = 'Timeline.Item';

export const Timeline = Object.assign(TimelineRoot, { Item: TimelineItem });
```

- [ ] **Step 6: Create `index.ts`**

```ts
export { Timeline } from './Timeline';
export type { TimelineProps, TimelineItemProps } from './Timeline';
```

- [ ] **Step 7: Run tests + typecheck + stylelint**

Run: `cd packages/design-system && npx vitest run src/components/Timeline/ && npx tsc --noEmit && npx stylelint "src/components/Timeline/*.scss"`
Expected: all green. Also `npx vitest run src/structure.test.ts` — may fail ONLY on the `src/index.ts` re-export (Task 2, expected; do NOT add it here).

- [ ] **Step 8: Commit**

```bash
git add packages/design-system/src/components/Timeline/
git commit -m "feat(Timeline): vertical activity-feed primitive (connector + node slots + compact)"
```

---

## Task 2: Exports + manifest

**Files:** Modify `src/index.ts`, `src/_meta/manifest.ts`, `scripts/generate-manifest.mjs`.

- [ ] **Step 1: `src/index.ts`** — add near the other Display exports:

```ts
export { Timeline } from './components/Timeline';
export type { TimelineProps, TimelineItemProps } from './components/Timeline';
```

- [ ] **Step 2:** Add `Timeline: 'Display'` to the `CLUSTERS` map in BOTH `src/_meta/manifest.ts` and `scripts/generate-manifest.mjs` (next to `Dot: 'Display',`). Keep identical.

- [ ] **Step 3: Regenerate + verify**

Run: `cd packages/design-system && npm run build:manifest && npx vitest run src/_meta/manifest.test.ts src/structure.test.ts && npx tsc --noEmit`
Expected: `components.manifest.json` gains a `Timeline` entry (`tier: "primitive"`, `cluster: "Display"`, `composes: []`); manifest + structure tests pass.

- [ ] **Step 4: Commit**

```bash
git add packages/design-system/src/index.ts packages/design-system/src/_meta/manifest.ts packages/design-system/scripts/generate-manifest.mjs packages/design-system/src/components.manifest.json
git commit -m "feat(Timeline): barrel export + manifest CLUSTERS (Display)"
```

---

## Task 3: Playground demo + wiring

**Files:** Create `TimelineDemo.tsx`; modify `App.tsx`, `navItems.ts`, `ComponentsIndex.tsx`, `registry.ts`.

- [ ] **Step 1: Create `packages/playground/src/pages/components/TimelineDemo.tsx`**

```tsx
import { Timeline, Avatar, Dot, Stack, Text, IconTile } from '@eocrm/design-system';
import { GitCommit, Mail, FileText } from 'lucide-react';
import { DemoLayout } from './DemoLayout';
import { Example } from './Example';
import { getComponentFiles } from '../../lib/componentFiles';

export function TimelineDemo() {
  return (
    <DemoLayout
      name="Timeline"
      componentName="Timeline"
      description="Vertical activity-feed primitive — a connector line running between per-item node slots (avatar / dot / icon) with content to the right; the line stops at the last node. Use compact for a dense sidebar widget. For a plain list use Stack."
      files={getComponentFiles('Timeline')}
    >
      <Example
        title="Activity feed (avatar / icon nodes)"
        description="Each item's node is a slot — an Avatar for a person, an icon tile for a system event. The connector links consecutive nodes and stops at the last."
        code={`<Timeline>
  <Timeline.Item node={<Avatar name="Dana Reyes" size="sm" />}>
    <Text size="sm"><strong>Dana Reyes</strong> · note · 2h ago</Text>
    <Text size="sm" tone="muted">Called the client; renewal confirmed for Q3.</Text>
  </Timeline.Item>
  …
</Timeline>`}
      >
        <Timeline>
          <Timeline.Item node={<Avatar name="Dana Reyes" size="sm" />}>
            <Stack gap="2xs">
              <Text size="sm">
                <strong>Dana Reyes</strong> · note · 2h ago
              </Text>
              <Text size="sm" tone="muted">
                Called the client; renewal confirmed for Q3.
              </Text>
            </Stack>
          </Timeline.Item>
          <Timeline.Item node={<IconTile icon={GitCommit} tone="info" size="sm" />}>
            <Stack gap="2xs">
              <Text size="sm">
                <strong>System</strong> · stage change · 5h ago
              </Text>
              <Text size="sm" tone="muted">
                Moved to “Negotiation”.
              </Text>
            </Stack>
          </Timeline.Item>
          <Timeline.Item node={<IconTile icon={Mail} tone="neutral" size="sm" />}>
            <Stack gap="2xs">
              <Text size="sm">
                <strong>Sam Cole</strong> · email · yesterday
              </Text>
              <Text size="sm" tone="muted">
                Sent the updated proposal.
              </Text>
            </Stack>
          </Timeline.Item>
          <Timeline.Item node={<Avatar name="Priya Nair" size="sm" />}>
            <Text size="sm">
              <strong>Priya Nair</strong> · created · 3d ago
            </Text>
          </Timeline.Item>
        </Timeline>
      </Example>

      <Example
        title="Compact (dot nodes — sidebar widget)"
        description="compact tightens the gutter, node box, and spacing; pair with small Dot nodes for a dense activity widget."
        code={`<Timeline compact>
  <Timeline.Item node={<Dot tone="success" />}>…</Timeline.Item>
</Timeline>`}
      >
        <div style={{ maxWidth: 260 }}>
          <Timeline compact>
            <Timeline.Item node={<Dot tone="info" />}>
              <Text size="sm">Renewal confirmed</Text>
              <Text size="xs" tone="muted">
                2h ago
              </Text>
            </Timeline.Item>
            <Timeline.Item node={<Dot tone="success" />}>
              <Text size="sm">Moved to Negotiation</Text>
              <Text size="xs" tone="muted">
                5h ago
              </Text>
            </Timeline.Item>
            <Timeline.Item node={<Dot tone="purple" />}>
              <Text size="sm">Proposal sent</Text>
              <Text size="xs" tone="muted">
                yesterday
              </Text>
            </Timeline.Item>
          </Timeline>
        </div>
      </Example>
    </DemoLayout>
  );
}
```

(Confirm props against the real APIs: `Avatar size="sm"`; `Dot tone` accepts `'info'|'success'|'purple'` (it's `BadgeTone`); `IconTile` accepts `icon`/`tone`/`size` — READ `IconTile.tsx` and adapt (e.g. if `IconTile` takes the icon as `children` not an `icon` prop, or its tone/size differ); `Stack` gap `'2xs'` may not exist (use `'xs'` if not). `lucide-react` icons `GitCommit`/`Mail`/`FileText` — adapt if absent. Don't invent props.)

- [ ] **Step 2: Wire the route** (`App.tsx`) — import + `<Route path="/components/timeline" element={<TimelineDemo />} />`.

- [ ] **Step 3: Wire the nav** (`layout/AppShell/navItems.ts`) — in the **Display** group, add (import `GitCommitVertical` from lucide-react; if absent use `Activity` or `ListTree`):

```ts
{ to: '/components/timeline', label: 'Timeline', icon: GitCommitVertical, end: false },
```

- [ ] **Step 4: Wire the overview card** (`ComponentsIndex.tsx`) — `Avatar` is already imported; add `Timeline` (and `Dot` if not imported) and a card with a small static timeline preview:

```tsx
{
  to: '/components/timeline',
  name: 'Timeline',
  description: 'Vertical activity feed — connector line through avatar/dot/icon nodes; compact variant.',
  preview: (
    <div style={{ width: '100%', maxWidth: 200 }}>
      <Timeline compact>
        <Timeline.Item node={<Dot tone="info" />}>
          <Text size="xs">Renewal confirmed</Text>
        </Timeline.Item>
        <Timeline.Item node={<Dot tone="success" />}>
          <Text size="xs">Moved to Negotiation</Text>
        </Timeline.Item>
      </Timeline>
    </div>
  ),
},
```

(Confirm `Dot`/`Text` are imported in `ComponentsIndex.tsx`; add any missing.)

- [ ] **Step 5: Wire the registry union** (`pages/mockups/registry.ts`) — add `| 'Timeline'` to the `ComponentName` union, after `| 'TimeField'`.

- [ ] **Step 6: Build the playground**

Run: `cd /Users/dpws/projects/design-system && make build`
Expected: typecheck + bundle succeed. Fix prettier drift with `npx prettier --write`.

- [ ] **Step 7: Commit**

```bash
git add packages/playground/src/pages/components/TimelineDemo.tsx packages/playground/src/App.tsx \
        packages/playground/src/layout/AppShell/navItems.ts packages/playground/src/pages/components/ComponentsIndex.tsx \
        packages/playground/src/pages/mockups/registry.ts
git commit -m "feat(Timeline): playground demo + nav/route/overview/registry wiring"
```

---

## Task 4: AGENTS.md

**Files:** Modify `packages/design-system/AGENTS.md`

- [ ] **Step 1: Add a TL;DR** in the Display area (grep for `Avatar`/`Dot`/`MediaTile`; match the heading depth):

````md
### `<Timeline>` — vertical activity-feed primitive

`<Timeline>` + `<Timeline.Item node>` — a connector line running between per-item `node` slots (`<Avatar>` / `<Dot>` / icon) with content to the right; the line stops at the last node. `<Timeline compact>` tightens it for a sidebar widget. Semantic `<ol>`/`<li>`. For a plain list use `<Stack>`.

```tsx
<Timeline>
  {activities.map((a) => (
    <Timeline.Item key={a.id} node={<Avatar name={a.actor} size="sm" />}>
      <Text size="sm">
        <strong>{a.actor}</strong> · {a.type} · {a.time}
      </Text>
      <Text size="sm" tone="muted">
        {a.body}
      </Text>
    </Timeline.Item>
  ))}
</Timeline>
```

- `node` is a SLOT (pass `<Dot>` / `<Avatar size="sm">` / a small icon) — there's no built-in dot.
- The last item's connector stops automatically (CSS `:last-child`); `compact` flows via CSS vars.
````

- [ ] **Step 2: Format + commit**

```bash
cd /Users/dpws/projects/design-system
npx prettier --write packages/design-system/AGENTS.md
git add packages/design-system/AGENTS.md
git commit -m "docs(Timeline): AGENTS.md TL;DR"
```

---

## Task 5: Full gates + browser verification

- [ ] **Step 1: Run all gates from the repo root**

```bash
cd /Users/dpws/projects/design-system
make test && make build-lib && make lint && npm run format:check
npm pack --workspace @eocrm/design-system --dry-run 2>&1 | grep -cE '\.test\.(t|j)sx?|\.spec\.|/types/|CLAUDE\.md|tsconfig'   # expect 0
```

Expected: all PASS; tarball grep `0`.

- [ ] **Step 2: Browser (Playwright, manual) on the running playground** (`/components/timeline`):
  - The Activity feed shows a vertical line connecting the nodes; the line is ABSENT below the LAST node (stops at it).
  - The line runs down the gutter center and touches each node's edges; nodes (avatar/icon) are aligned to the gutter.
  - The compact example shows tighter spacing + smaller dot nodes, same last-node-stop.
  - No console errors; nothing overflows.

---

## Self-review notes

- **Spec coverage:** compound `<Timeline>`/`<Timeline.Item node>` (T1) · connector touches node edges via the fixed nodeBox + absolute line (T1) · last-node stop via `:last-child` (T1) · compact via CSS-var cascade (T1) · `<ol>`/`<li>` semantics + `aria-hidden` connector (T1) · tokens (T1) · exports + manifest `Display`/`primitive` (T2) · demo + wiring (T3) · JSDoc `@remarks` (T1) · AGENTS (T4) · no i18n. All spec sections map to a task.
- **Type consistency:** `TimelineProps { compact?; children }`, `TimelineItemProps { node; children }` defined in `Timeline.tsx` (T1), re-exported (T1 index → T2 barrel). `Object.assign(TimelineRoot, { Item })` compound shape.
- **Rule 4:** only the component's internal grid + the connector's `position:absolute` in the `position:relative` gutter + the `<ol>` UA reset — file-level justified `stylelint-disable` (mirrors SortableGroup/Image). No consumer layout props.
- **Adapt-point:** the demo (T3) assumes `IconTile` `icon`/`tone`/`size` props, `Dot tone` values, `Stack gap="2xs"`, and lucide icon names — the task instructs the implementer to read the real APIs and adapt. The component itself (T1–T2) has no such uncertainty.
