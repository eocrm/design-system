# Dark Categorical Palette Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add dark-theme variants for the 30-color categorical `--color-palette-*` set (60 tokens) so the palette participates in dark mode like the rest of the system.

**Architecture:** Add the 60 `--color-palette-<name>-bg/-fg` overrides as one block inside the **existing** `dark-tokens` mixin in `src/styles/dark.scss`. The mixin is already emitted into both dark scopes (`:root[data-theme='dark']` + the `prefers-color-scheme: dark` media query), so the palette flips under forced-dark and system-dark automatically, and component `var()`-aliases (`IconTile`, `Checkbox`, `OptionsPicker`, Badge palette tones) cascade with no changes. Values were generated once by a fixed HSL recipe (same hue, saturation preserved, lightness flipped) and are committed as literal hex.

**Tech Stack:** SCSS (`@mixin`), CSS custom properties, the existing `dark.scss` token layer; Playwright (empirical AA verification).

**Spec:** `docs/superpowers/specs/2026-06-07-dark-palette-design.md`

**Branch:** `feat/dark-palette` (already checked out; spec commit `bf17c4b` is on it).

---

## Notes for the implementer (read first)

- **CSS-only, no new file, no API.** This adds 60 custom-property declarations to one existing mixin + two doc edits. No TS, no test files, no component changes, no manifest, no exports. `PaletteColor` / `PALETTE_COLORS` are unchanged.
- **No unit test.** Token values aren't unit-tested in this repo (`dark.scss` has none; jsdom doesn't apply CSS-module/`:root` styles). Verification is `make lint` + `make build` (SCSS compiles) + the **Playwright dark sweep** (Task 2). Do not fabricate a token-value unit test.
- **stylelint:** custom-property declarations (`--x: #hex`) are exempt from `declaration-strict-value`, so raw hex is allowed here (same as the existing Badge block above it). The section `//` comment needs a blank line before it (`scss/double-slash-comment-empty-line-before`) — the verbatim block below already has it.
- **Commit through a PR** (root CLAUDE.md). Push once, after Task 2, via the PR. This is a `packages/design-system/**` change → run the Hard-rule-8 review (Task 2).

---

## File Structure

| File                                          | Action | Responsibility                                                                                                                                                                                                               |
| --------------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/design-system/src/styles/dark.scss` | Modify | Add the 60-decl `--color-palette-*` block inside `@mixin dark-tokens` (after the Tooltip block, before the mixin's closing `}`); update the file header comment to include the palette in the "literal-hex exceptions" list. |
| `packages/design-system/AGENTS.md`            | Modify | Dark theme section "Out of scope" line: drop the `--color-palette-*` exclusion (now covered).                                                                                                                                |

---

## Task 1: Dark palette tokens (`dark.scss`) + docs

**Files:**

- Modify: `packages/design-system/src/styles/dark.scss`
- Modify: `packages/design-system/AGENTS.md`

- [ ] **Step 1: Insert the 60-declaration palette block into the `dark-tokens` mixin**

In `packages/design-system/src/styles/dark.scss`, the `@mixin dark-tokens { … }` block currently ends with the Tooltip tokens:

```scss
  --tooltip-bg: #39424b;
  --tooltip-fg: #f0f3f6;
  --tooltip-arrow-bg: #39424b;
}
```

Replace that exact snippet with the same three Tooltip lines PLUS the palette block, keeping the mixin's closing `}`:

```scss
  --tooltip-bg: #39424b;
  --tooltip-fg: #f0f3f6;
  --tooltip-arrow-bg: #39424b;

  // Categorical palette — the 30 named colors (a bg + fg pair each). Same hue
  // as light, lightness flipped (dark tinted bg + light vivid fg), saturation
  // preserved so muted colors (slate / stone / charcoal / taupe) stay muted.
  // All 30 fg/bg pairs are AA ≥ 7:1. Generated by the HSL recipe in
  // docs/superpowers/specs/2026-06-07-dark-palette-design.md.
  --color-palette-red-bg: #482219;
  --color-palette-red-fg: #f9a994;
  --color-palette-coral-bg: #482619;
  --color-palette-coral-fg: #f2b39b;
  --color-palette-orange-bg: #483219;
  --color-palette-orange-fg: #f9c994;
  --color-palette-amber-bg: #483919;
  --color-palette-amber-fg: #f9d994;
  --color-palette-gold-bg: #483c19;
  --color-palette-gold-fg: #f9e194;
  --color-palette-yellow-bg: #484219;
  --color-palette-yellow-fg: #f9ee94;
  --color-palette-olive-bg: #414819;
  --color-palette-olive-fg: #ebf994;
  --color-palette-lime-bg: #344819;
  --color-palette-lime-fg: #cef994;
  --color-palette-green-bg: #194830;
  --color-palette-green-fg: #94f9c7;
  --color-palette-emerald-bg: #194839;
  --color-palette-emerald-fg: #94f9d9;
  --color-palette-mint-bg: #19483d;
  --color-palette-mint-fg: #94f9e2;
  --color-palette-teal-bg: #194548;
  --color-palette-teal-fg: #94f3f9;
  --color-palette-cyan-bg: #194048;
  --color-palette-cyan-fg: #94e8f9;
  --color-palette-sky-bg: #193048;
  --color-palette-sky-fg: #a4c7ea;
  --color-palette-blue-bg: #192c48;
  --color-palette-blue-fg: #94bdf9;
  --color-palette-navy-bg: #192648;
  --color-palette-navy-fg: #a6b8e8;
  --color-palette-indigo-bg: #191a48;
  --color-palette-indigo-fg: #acace2;
  --color-palette-violet-bg: #201948;
  --color-palette-violet-fg: #b0a8e6;
  --color-palette-lavender-bg: #251e43;
  --color-palette-lavender-fg: #bab2dc;
  --color-palette-purple-bg: #201948;
  --color-palette-purple-fg: #b3abe3;
  --color-palette-plum-bg: #441c45;
  --color-palette-plum-fg: #deafdf;
  --color-palette-fuchsia-bg: #481943;
  --color-palette-fuchsia-fg: #eaa4e3;
  --color-palette-magenta-bg: #481935;
  --color-palette-magenta-fg: #eea0cf;
  --color-palette-pink-bg: #48192a;
  --color-palette-pink-fg: #f19dbb;
  --color-palette-rose-bg: #481923;
  --color-palette-rose-fg: #ef9eaf;
  --color-palette-brown-bg: #483419;
  --color-palette-brown-fg: #e6cba8;
  --color-palette-taupe-bg: #3b3026;
  --color-palette-taupe-fg: #d3c7bb;
  --color-palette-slate-bg: #242d3d;
  --color-palette-slate-fg: #b9c2d5;
  --color-palette-stone-bg: #33312d;
  --color-palette-stone-fg: #cac7c3;
  --color-palette-charcoal-bg: #2c3035;
  --color-palette-charcoal-fg: #c1c7cc;
}
```

- [ ] **Step 2: Update the `dark.scss` header comment to list the palette as a covered exception**

In the same file, the header comment currently reads:

```scss
// overrides are the literal-hex exceptions — Badge filled tones and the
// inverted Tooltip — kept here so the whole dark palette is one reviewable
// surface. Values are contrast-targeted for WCAG AA and tunable live via the
// playground toggle.
```

Replace with:

```scss
// overrides are the literal-hex exceptions — Badge filled tones, the inverted
// Tooltip, and the categorical --color-palette-* set — kept here so the whole
// dark palette is one reviewable surface. Values are contrast-targeted for
// WCAG AA and tunable live via the playground toggle.
```

- [ ] **Step 3: Update the AGENTS "Out of scope" line**

In `packages/design-system/AGENTS.md`, the Dark theme section has this line:

```markdown
**Out of scope (theme-independent by design):** the 30-color categorical `--color-palette-*` set (a dark variant is a planned fast-follow), avatar identity colors, and BrandIcon brand marks.
```

Replace it with:

```markdown
**Out of scope (theme-independent by design):** avatar identity colors and BrandIcon brand marks. (The 30-color categorical `--color-palette-*` set now flips in dark too — same hue, dark tinted bg + light fg.)
```

- [ ] **Step 4: Run lint (SCSS compiles cleanly)**

Run: `make lint`
Expected: PASS. (If `scss/double-slash-comment-empty-line-before` fires, the section comment lost its leading blank line — re-add it.)

- [ ] **Step 5: Build (the SCSS actually compiles via Vite/Sass)**

Run: `make build`
Expected: PASS — the playground bundles, proving `dark.scss` compiles with the new block.

- [ ] **Step 6: Build the library typecheck + run tests (confirm nothing regressed)**

Run: `make build-lib`
Expected: PASS.

Run: `make test`
Expected: PASS — no test touches palette token values; this confirms nothing regressed.

- [ ] **Step 7: Format check**

Run: `npm run format:check`
Expected: PASS (run `npm run format` first if prettier wants to touch AGENTS.md, then re-check).

- [ ] **Step 8: Commit**

```bash
git add packages/design-system/src/styles/dark.scss packages/design-system/AGENTS.md
git commit -m "feat(theme): dark variants for the categorical --color-palette-* set"
```

Do NOT push.

---

## Task 2: Verification — Playwright dark sweep + Rule-8

**Files:** none (verification + any nudges get their own commits).

- [ ] **Step 1: Ensure the dev server is up**

Run: `make dev` (if not already serving http://localhost:8080).

- [ ] **Step 2: Force dark, sweep all 30 palette swatches**

Via Playwright: navigate to `http://localhost:8080/components/palette`, set `localStorage['eocrm-playground-theme']='dark'`, reload, confirm `document.documentElement.dataset.theme === 'dark'`. Then read the 30 resolved pairs and assert each:

```js
() => {
  const NAMES = [
    'red',
    'coral',
    'orange',
    'amber',
    'gold',
    'yellow',
    'olive',
    'lime',
    'green',
    'emerald',
    'mint',
    'teal',
    'cyan',
    'sky',
    'blue',
    'navy',
    'indigo',
    'violet',
    'lavender',
    'purple',
    'plum',
    'fuchsia',
    'magenta',
    'pink',
    'rose',
    'brown',
    'taupe',
    'slate',
    'stone',
    'charcoal',
  ];
  const cs = getComputedStyle(document.documentElement);
  const rgb = (c) => {
    const m = (c || '').match(/[\d.]+/g).map(Number);
    return [m[0], m[1], m[2]];
  };
  const lin = (v) => {
    v /= 255;
    return v <= 0.03928 ? v / 12.92 : Math.pow((v + 0.055) / 1.055, 2.4);
  };
  const L = ([r, g, b]) => 0.2126 * lin(r) + 0.7152 * lin(g) + 0.0722 * lin(b);
  // resolve a hex token to actual rgb via a probe element
  const probe = document.createElement('span');
  document.body.appendChild(probe);
  const resolve = (varName) => {
    probe.style.color = `var(${varName})`;
    return getComputedStyle(probe).color;
  };
  const out = NAMES.map((n) => {
    const bg = rgb(resolve(`--color-palette-${n}-bg`));
    const fg = rgb(resolve(`--color-palette-${n}-fg`));
    const hi = Math.max(L(fg), L(bg)),
      lo = Math.min(L(fg), L(bg));
    const ratio = (hi + 0.05) / (lo + 0.05);
    const nearWhiteBg = bg.every((c) => c >= 200);
    return { n, bg, fg, ratio: Math.round(ratio * 100) / 100, passAA: ratio >= 4.5, nearWhiteBg };
  });
  probe.remove();
  return {
    theme: document.documentElement.dataset.theme,
    fails: out.filter((o) => !o.passAA || o.nearWhiteBg),
    all: out,
  };
};
```

Expected: `theme === 'dark'`, `fails` is empty (every pair AA ≥ 4.5 and no near-white bg). Take a screenshot of the palette grid for the visual record.

- [ ] **Step 3: Cross-check a consumer (IconTile flips)**

Navigate to `/mockups/dashboard` in dark; confirm the palette-colored `IconTile` (the `#deebff` light-blue tile flagged in the v0.1.21 sweep) now renders a dark tile (read its computed `background-color` — it should be a dark value, e.g. `rgb(25, 44, 72)` for `--color-palette-blue-bg: #192c48`, or whatever palette color it uses).

- [ ] **Step 4: Light mode unaffected**

Set `localStorage['eocrm-playground-theme']='light'`, reload `/components/palette`, confirm a swatch resolves to its LIGHT value (e.g. `--color-palette-red-bg` → `rgb(255, 235, 230)` = `#ffebe6`). Proves the override is dark-scoped only.

- [ ] **Step 5: Library Rule-8 review loop**

Gates first (`npm test`, `npm run typecheck`, `npm run lint:css`, `npm run build`, `npm pack --dry-run -w @eocrm/design-system`), then a fresh-context reviewer over the `dark.scss` + `AGENTS.md` change, briefed on the 10 categories. Fix every Critical + Important, re-run, re-review until `clean enough to stop`.

- [ ] **Step 6: Nudge + recommit any failures**

If a swatch fails AA or reads wrong, adjust its hex in `dark.scss`, re-run `make lint && make build`, re-sweep, commit with a focused message.

---

## Task 3: Open the PR

**Files:** none.

- [ ] **Step 1: Record pre-merge version, push, open PR**

```bash
cd /Users/dpws/projects/design-system
git fetch --tags --force origin -q
PREV=$(git tag --list 'v*' --sort=-v:refname | head -1); echo "PREV=$PREV"
git push -u origin feat/dark-palette
gh pr create --repo eocrm/design-system --base main \
  --title "feat: dark variants for the categorical color palette" \
  --body "$(cat <<'EOF'
## Summary
- Adds dark-theme values for all 30 `--color-palette-*` colors (60 tokens), the fast-follow scoped out of the v0.1.21 dark theme.
- One block inside the existing `dark.scss` `dark-tokens` mixin → flips under forced-dark + system-dark; `IconTile` / `Checkbox` / `OptionsPicker` / Badge palette tones cascade for free.
- Recipe: same hue, saturation preserved (muted colors stay muted), lightness flipped (dark tinted bg + light fg). All pairs AA ≥ 7:1.
- Resolves the one out-of-scope artifact from the v0.1.21 sweep (palette-colored `IconTile` rendered a light tile in dark).

## Test plan
- [ ] `make test`, `make build`, `make build-lib`, `make lint`, `npm run format:check`, `npm pack --dry-run`
- [ ] Playwright `/components/palette` in dark: all 30 swatches resolve to dark hex, fg/bg AA ≥ 4.5, no near-white bg; light mode still shows the light swatches
- [ ] Library Rule-8 review clean

## Notes
- No TS/API change (`PaletteColor` unchanged); avatar palette stays theme-independent.
EOF
)"
```

- [ ] **Step 2: Watch the gate, merge**

```bash
PR=$(gh pr view --repo eocrm/design-system --json number -q .number)
gh pr checks "$PR" --repo eocrm/design-system --watch
# if BEHIND: gh pr update-branch --repo eocrm/design-system "$PR" && re-watch
gh pr merge "$PR" --repo eocrm/design-system --squash --delete-branch
```

- [ ] **Step 3: Confirm the auto-release**

```bash
MERGE_SHA=$(gh pr view "$PR" --repo eocrm/design-system --json mergeCommit -q .mergeCommit.oid)
RUN_ID=$(gh run list --repo eocrm/design-system --workflow=Release --commit "$MERGE_SHA" --json databaseId --jq '.[0].databaseId')
gh run watch "$RUN_ID" --repo eocrm/design-system --exit-status
gh run view "$RUN_ID" --repo eocrm/design-system --json jobs --jq '.jobs[]|"\(.name): \(.conclusion)"'
git fetch --tags --force origin -q && git describe --tags --abbrev=0   # new version
```

Expected: `publish: success`; new `v*` tag (one patch above `PREV`).

- [ ] **Step 4: Realign local main**

```bash
git checkout main && git fetch origin && git reset --hard origin/main
```

---

## Self-Review (completed by plan author)

**1. Spec coverage:**

- 60 dark `--color-palette-*` overrides (exact hex) → Task 1 Step 1 (verbatim, all 30 pairs from the spec table). ✅
- Recipe / saturation-preserved values → encoded in the committed hex; comment references the spec. ✅
- One block in the existing `dark-tokens` mixin (both scopes, components cascade) → Task 1 Step 1. ✅
- Header comment + AGENTS "out of scope" update → Task 1 Steps 2–3. ✅
- No TS/API/test/light-palette change → respected (only `dark.scss` + `AGENTS.md` touched). ✅
- Verification (Playwright sweep: dark hex + AA + no near-white; IconTile cross-check; light unaffected; Rule-8) → Task 2. ✅
- Delivery (PR → auto-release → realign) → Task 3. ✅
- Non-goals (light palette, TS surface, avatar palette, runtime computation) → none touched. ✅

**2. Placeholder scan:** No TBD/TODO/"handle edge cases". All 60 hex values are literal; the verification script is complete. ✅

**3. Type/name consistency:** Token names follow `--color-palette-<name>-bg/-fg` exactly matching `tokens.scss` and the 30 `PALETTE_COLORS` names; the Playwright `NAMES` array matches the same 30 in order. The header-comment and AGENTS edits quote the current file text verbatim. ✅
