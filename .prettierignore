# Build output
dist/
dist-ssr/
**/dist/
**/.gradle/
**/build/
*.tsbuildinfo

# Dependencies
node_modules/

# Lockfiles — managed by the package manager
package-lock.json

# Vite caches
.vite/

# Tooling caches
.playwright-mcp/

# Screenshots captured by dev workflow
*.png

# Generated by packages/design-system/scripts/generate-manifest.mjs — must
# match the generator's JSON.stringify output verbatim, or every later PR
# carries hundreds of lines of reformat churn it did not cause.
#
# Two things this entry does NOT do. It is resolved relative to prettier's CWD,
# so `prettier --write` run from inside packages/design-system ignores it
# entirely and reflows the file. CWD is the only variable — naming the file
# explicitly does not help. Run prettier from the repo root.
# And the drift test in src/_meta/manifest.test.ts compares
# PARSED objects, which is blind to formatting; the byte comparison beside it
# is what actually catches this.
packages/design-system/src/components.manifest.json
